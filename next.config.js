// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes agregar opciones personalizadas en el futuro
  // Por ahora, usamos la configuración por defecto
  reactStrictMode: true, // Buena práctica
};

module.exports = nextConfig;